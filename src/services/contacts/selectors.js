/** @format */

export const selContacts = (state) => state.contacts.items;
export const selEditItem = (state) => state.contacts.editItem;
export const selIsLoading = (state) => state.loading.state;
export const selIsEditing = (state) => state.contacts.isEdit;
